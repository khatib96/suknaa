# UX / Product Backlog

> ملف دائم لملاحظات المنتج وتجربة المستخدم والمسميات. هذه الملاحظات ليست blockers على Phase 2 M9 إلا إذا نُقلت صراحة إلى milestone لاحق.

## 2026-05-07 — ملاحظات بعد Smoke Test لـ M9

- [ ] واجهة KYC لازم تصير حسب نوع المضيف. الحساب الشخصي لا يجب أن يرى حقول الشركات أو الفنادق غير المطلوبة له.
  - ملاحظة 2026-05-08: `/api/me` لا يعرض `hostProfile.hostSubtype` حالياً، لذلك تم تنظيف النصوص وإخفاء حقل الفنادق فقط. التخصيص الكامل يحتاج API لاحق.
- [x] مراجعة هل صورة السيلفي مع الهوية مطلوبة فعلاً للحساب الشخصي. إذا بقيت مطلوبة، يجب شرح السبب بوضوح في الواجهة.
- [x] تعديل مسميات نوع الحساب:
  - `individual` -> `شخصي`
  - مسارات الشركات/المكاتب -> `تجاري`
- [x] فصل تسجيل الفنادق عن تسجيل بيوت العطلات. onboarding الفنادق يجب أن يظهر كمسار hospitality/company مستقل.
  - ملاحظة 2026-05-08: تم إظهار مسار الفنادق ككرت disabled "قريباً" بدون حذف enum أو تغيير backend.
- [x] تعديل التصنيف العام في الواجهة عند اللزوم:
  - استبدال `عقارات` بـ `بيوت عطلات`
  - إبقاء مسميات الفنادق منفصلة عن بيوت العطلات.
- [x] مراجعة كل مسميات حقول host/KYC. بعض المسميات الحالية صحيحة تقنياً لكنها تحتاج صياغة عربية أوضح قبل الإنتاج.
- [x] تسجيل دخول الضيف يوجّه حالياً إلى `/dashboard`، وهذا خارج M9 ويرجع 404. نحتاج fallback مؤقت إلى أن توجد صفحة dashboard/profile الحقيقية.
- اللغة/i18n مؤجلة إلى آخر مرحلة إعداد الواجهة بعد ثبات بنية الموقع حتى لا تزيد ضغط العمل الآن.
- [ ] dashboard/profile UI ليست جزءاً من إغلاق Phase 2 foundation. يجب تخطيطها لاحقاً ضمن dashboard/host/admin phases.
  - ملاحظة 2026-05-08: أضيفت صفحات placeholder فقط لـ `/dashboard` و`/host/dashboard` لمنع 404. الداشبورد الحقيقي لاحق.
- موافقة KYC حالياً تتم عبر API/admin service:
  - `GET /v1/admin/kyc/queue`
  - `POST /v1/admin/kyc/:id/approve`
  - `POST /v1/admin/kyc/:id/reject`
  واجهة admin للمراجعة لاحقة وليست شرطاً على M10.

## قاعدة التعامل

- إبقاء Phase 2 M9 مغلقاً.
- نقل أي ملاحظة من هذا الملف إلى خطة milestone لاحق فقط عند بدء تنفيذها.
- عدم خلط هذه الملاحظات مع مهام أمان auth/token/cookies.
